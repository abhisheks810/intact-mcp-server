# Implementation Result: host map-platform loop

Created: 2026-05-14T21:40:27.953662Z
Status: completed_with_blockers

## Summary

Tool-loop run finished.

## Verification

- $ bash ./scripts/loop-preflight.sh --require-clean
cwd: /Users/abhisheksrivastava/map_platform
exit: 0
stdout:
-- DNS check (github.com)
OK
-- Git remote auth/connectivity check
OK
-- Canonical repo cleanliness check
OK
Preflight checks passed.
- Tool-loop actions:
- [
  {
    "action": {
      "tool": "read_map_platform_file",
      "args": {
        "path": "tests/test_route_contracts.py"
      },
      "reason": "Custom router and route QA are the best bounded next focus for the routing agent. Reading the full route contract test file is the fastest way to identify a small, commit-worthy reliability gap before proposing or editing any router/backend code."
    }
  },
  {
    "tool_result": {
      "tool": "read_map_platform_file",
      "ok": true,
      "output": "import unittest\nfrom unittest.mock import patch\n\nimport httpx\ntry:\n    from fastapi import HTTPException\nexcept ModuleNotFoundError:  # pragma: no cover\n    HTTPException = None\n\ntry:\n    from backend.routers import route as route_module\nexcept ModuleNotFoundError as e:  # pragma: no cover\n    if getattr(e, \"name\", \"\") == \"fastapi\":\n        route_module = None\n    else:\n        raise\n\n\nclass _DummyResponse:\n    def __init__(self, status_code=200, payload=None):\n        self.status_code = status_code\n        self._payload = payload or {}\n        self.request = httpx.Request(\"GET\", \"http://example.test\")\n\n    def raise_for_status(self):\n        if self.status_code >= 400:\n            raise httpx.HTTPStatusError(\n                \"error\",\n                request=self.request,\n                response=httpx.Response(self.status_code, request=self.request),\n            )\n\n    def json(self):\n        return self._payload\n\n\nclass _DummyClient:\n    def __init__(self, response=None, error=None):\n        self._response = response or _DummyResponse()\n        self._error = error\n\n    def __enter__(self):\n        return self\n\n    def __exit__(self, exc_type, exc, tb):\n        return False\n\n    def get(self, *_args, **_kwargs):\n        if self._error:\n            raise self._error\n        return self._response\n\n\n@unittest.skipIf(HTTPException is None or route_module is None, \"fastapi not installed in this environment\")\nclass RouteContractsTests(unittest.TestCase):\n    def test_parse_router_targets_defaults_to_single_host(self):\n        with patch.object(route_module, \"OSRM_ROUTER_HOSTS\", \"\"), patch.object(\n            route_module, \"ROUTER_HOST\", \"http://router:5000\"\n        ):\n            targets = route_module.parse_router_targets()\n        self.assertEqual(targets, [(\"default\", \"http://router:5000\")])\n\n    def test_parse_router_targets_parses_named_and_unnamed_tokens(self):\n        hosts = \" north=http://a:5000/ , http://b:5000 , =http://c:5000 , bad= \"\n        with patch.object(route_module, \"OSRM_ROUTER_HOSTS\", hosts), patch.object(\n            route_module, \"ROUTER_HOST\", \"http://router:5000\"\n        ):\n            targets = route_module.parse_router_targets()\n        self.assertEqual(\n            targets,\n            [(\"north\", \"http://a:5000\"), (\"router2\", \"http://b:5000\"), (\"router3\", \"http://c:5000\")],\n        )\n\n    def test_to_latlon_parses_expected_format(self):\n        lat, lon = route_module.to_latlon(\"28.6129,77.2295\")\n        self.assertAlmostEqual(lat, 28.6129)\n        self.assertAlmostEqual(lon, 77.2295)\n\n    def test_to_latlon_rejects_invalid_format(self):\n        with self.assertRaises(ValueError):\n            route_module.to_latlon(\"Delhi\")\n\n    def test_route_rejects_invalid_query_format(self):\n        with self.assertRaises(HTTPException) as ctx:\n            route_module.route(origin=\"Delhi\", destination=\"Mumbai\")\n        self.assertEqual(ctx.exception.status_code, 400)\n\n    def test_route_uses_intact_provider_when_configured(self):\n        with patch.object(route_module, \"ROUTER_PROVIDER\", \"intact\"), patch.object(\n            route_module, \"route_with_intact\", return_value={\"provider\": \"intact\"}\n        ) as intact_mock:\n            result = route_module.route(\"28.6,77.2\", \"26.8,80.9\")\n        self.assertEqual(result[\"provider\"], \"intact\")\n        intact_mock.assert_called_once()\n\n    def test_route_rejects_unsupported_provider(self):\n        with patch.object(route_module, \"ROUTER_PROVIDER\", \"unsupported\"):\n            with self.assertRaises(HTTPException) as ctx:\n                route_module.route(\"28.6,77.2\", \"26.8,80.9\")\n        self.assertEqual(ctx.exception.status_code, 500)\n        self.assertIn(\"Unsupported ROUTER_PROVIDER\", ctx.exception.detail)\n\n    def test_route_with_intact_success_shape(self):\n        payload = {\"provider\": \"intact\", \"distance_km\": 1.2}\n        with patch(\"backend.routers.route.httpx.Client\", return_value=_DummyClient(response=_DummyResponse(payload=payload))):\n            result = route_module.route_with_intact(\"28.6,77.2\", \"26.8,80.9\")\n        self.assertEqual(result, payload)\n\n    def test_route_with_intact_request_error(self):\n        err = httpx.RequestError(\"down\", request=httpx.Request(\"GET\", \"http://custom:8090\"))\n        with patch.object(route_module, \"CUSTOM_ROUTER_HOST\", \"http://custom:8090\"), patch(\n            \"backend.routers.route.httpx.Client\", return_value=_DummyClient(error=err)\n        ):\n            result = route_module.route_with_intact(\"28.6,77.2\", \"26.8,80.9\")\n        self.assertIn(\"Custom router unavailable\", result[\"error\"])\n\n    def test_route_with_intact_http_error(self):\n        with patch(\n            \"backend.routers.route.httpx.Client\",\n            return_value=_DummyClient(response=_DummyResponse(status_code=502)),\n        ):\n            result = route_module.route_with_intact(\"28.6,77.2\", \"26.8,80.9\")\n        self.assertEqual(result[\"error\"], \"Custom router error (502).\")\n\n    def test_nearest_snap_distance_success(self):\n        payload = {\"code\": \"Ok\", \"waypoints\": [{\"distance\": 123.4}]}\n        with patch(\"backend.routers.route.httpx.Client\", return_value=_DummyClient(response=_DummyResponse(payload=payload))):\n            with httpx.Client() as client:\n                distance = route_module.nearest_snap_distance(client, \"http://router:5000\", 28.6, 77.2)\n        self.assertEqual(distance, 123.4)\n\n    def test_nearest_snap_distance_rejects_non_ok_code(self):\n        payload = {\"code\": \"NoSegment\", \"waypoints\": [{\"distance\": 1.0}]}\n        with patch(\"backend.routers.route.httpx.Client\", return_value=_DummyClient(response=_DummyResponse(payload=payload))):\n            with httpx.Client() as client:\n                with self.assertRaises(ValueError):\n                    route_module.nearest_snap_distance(client, \"http://router:5000\", 28.6, 77.2)\n\n    def test_nearest_snap_distance_rejects_missing_waypoints(self):\n        payload = {\"code\": \"Ok\", \"waypoints\": []}\n        with patch(\"backend.routers.route.httpx.Client\", return_value=_DummyClient(response=_DummyResponse(payload=payload))):\n            with httpx.Client() as client:\n                with self.assertRaises(ValueError):\n                    route_module.nearest_snap_distance(client, \"http://router:5000\", 28.6, 77.2)\n\n    def test_pick_osrm_target_prefers_in_coverage_candidate(self):\n        with patch.object(route_module, \"ROUTER_TARGETS\", [(\"a\", \"h1\"), (\"b\", \"h2\")]), patch.object(\n            route_module, \"OSRM_MAX_SNAP_DISTANCE_M\", 100.0\n        ), patch.object(\n            route_module, \"OSRM_RELAXED_MAX_SNAP_DISTANCE_M\", 200.0\n        ), patch.object(\n            route_module, \"nearest_snap_distance\", side_effect=[300.0, 300.0, 50.0, 60.0]\n        ), patch(\n            \"backend.routers.route.httpx.Client\", return_value=_DummyClient()\n        ):\n            target, diagnostics = route_module.pick_osrm_target(28.6, 77.2, 26.8, 80.9)\n        self.assertEqual(target[0], \"b\")\n        self.assertFalse(target[4])\n        self.assertEqual(len(diagnostics), 2)\n\n    def test_pick_osrm_target_uses_relaxed_candidate(self):\n        with patch.object(route_module, \"ROUTER_TARGETS\", [(\"a\", \"h1\")]), patch.object(\n            route_module, \"OSRM_MAX_SNAP_DISTANCE_M\", 100.0\n        ), patch.object(\n            route_module, \"OSRM_RELAXED_MAX_SNAP_DISTANCE_M\", 200.0\n        ), patch.object(\n            route_module, \"nearest_snap_distance\", side_effect=[150.0, 160.0]\n        ), patch(\n            \"backend.routers.route.httpx.Client\", return_value=_DummyClient()\n        ):\n            target, _ = route_module.pick_osrm_target(28.6, 77.2, 26.8, 80.9)\n        self.assertEqual(target[0], \"a\")\n        self.assertTrue(target[4])\n\n    def test_pick_osrm_target_returns_none_on_errors(self):\n        with patch.object(route_module, \"ROUTER_TARGETS\", [(\"a\", \"h1\")]), patch.object(\n            route_module, \"nearest_snap_distance\", side_effect=RuntimeError(\"down\")\n        ), patch(\"backend.routers.route.httpx.Client\", return_value=_DummyClient()):\n            target, diagnostics = route_module.pick_osrm_target(28.6, 77.2, 26.8, 80.9)\n        self.assertIsNone(target)\n        self.assertIn(\"error\", diagnostics[0])\n\n    def test_route_with_osrm_uses_selected_target(self):\n        target = (\"north\", \"http://router:5000\", 10.0, 15.0, False)\n        with patch.object(route_module, \"pick_osrm_target\", return_value=(target, [])), patch.object(\n            route_module,\n            \"run_osrm_route\",\n            return_value={\"provider\": \"osrm\", \"router_name\": \"north\"},\n        ) as run_mock:\n            result = route_module.route_with_osrm(\"28.6,77.2\", \"26.8,80.9\", 28.6, 77.2, 26.8, 80.9)\n        self.assertEqual(result[\"provider\"], \"osrm\")\n        self.assertEqual(result[\"router_name\"], \"north\")\n        run_mock.assert_called_once()\n\n    def test_route_with_osrm_returns_global_unavailable(self):\n        with patch.object(route_module, \"pick_osrm_target\", return_value=(None, [])), patch.object(\n            route_module, \"OSRM_GLOBAL_ROUTER_HOST\", \"http://host.docker.internal:5007\"\n        ), patch.object(\n            route_module,\n            \"nearest_snap_distance\",\n            side_effect=RuntimeError(\"global router down\"),\n        ), patch(\"backend.routers.route.httpx.Client\", return_value=_DummyClient()):\n            result = route_module.route_with_osrm(\"28.6,77.2\", \"17.3,78.4\", 28.6, 77.2, 17.3, 78.4)\n        self.assertEqual(result[\"error\"], \"Global OSRM unavailable at http://host.docker.internal:5007.\")\n\n    def test_route_with_osrm_returns_outside_coverage_shape(self):\n        diagnostics = [{\"router\": \"north\", \"in_coverage\": False}]\n        with patch.object(route_module, \"pick_osrm_target\", return_value=(None, diagnostics)), patch.object(\n            route_module, \"OSRM_GLOBAL_ROUTER_HOST\", \"\"\n        ):\n            result = route_module.route_with_osrm(\"28.6,77.2\", \"17.3,78.4\", 28.6, 77.2, 17.3, 78.4)\n        self.assertEqual(result[\"code\"], \"outside_osrm_coverage\")\n        self.assertIn(\"router_diagnostics\", result)\n        self.assertEqual(result[\"router_diagnostics\"], diagnostics)\n\n    def test_route_with_osrm_global_success_path(self):\n        with patch.object(route_module, \"pick_osrm_target\", return_value=(None, [])), patch.object(\n            route_module, \"OSRM_GLOBAL_ROUTER_HOST\", \"http://host.docker.internal:5007\"\n        ), patch.object(\n            route_module, \"OSRM_GLOBAL_ROUTER_NAME\", \"india-merged\"\n        ), patch.object(\n            route_module, \"nearest_snap_distance\", side_effect=[12.0, 13.0]\n        ), patch.object(\n            route_module, \"run_osrm_route\", return_value={\"router_name\": \"india-merged\", \"used_global_fallback\": True}\n        ) as run_mock, patch(\n            \"backend.routers.route.httpx.Client\", return_value=_DummyClient()\n        ):\n            result = route_module.route_with_osrm(\"28.6,77.2\", \"17.3,78.4\", 28.6, 77.2, 17.3, 78.4)\n        self.assertEqual(result[\"router_name\"], \"india-merged\")\n        self.assertTrue(result[\"used_global_fallback\"])\n        run_mock.assert_called_once()\n\n    def test_run_osrm_route_success_shape(self):\n        payload = {\n            \"routes\": [\n                {\n                    \"distance\": 1200.0,\n                    \"duration\": 300.0,\n                    \"legs\": [{\"steps\": [{\"maneuver\": {\"instruction\": \"Head north\"}}]}],\n                    \"geometry\": {\"coordinates\": [[77.2, 28.6], [80.9, 26.8]]},\n                }\n            ],\n            \"waypoints\": [{\"distance\": 12.0}, {\"distance\": 16.0}],\n        }\n        with patch(\"backend.routers.route.httpx.Client\", return_value=_DummyClient(response=_DummyResponse(payload=payload))):\n            result = route_module.run_osrm_route(\n                origin=\"28.6,77.2\",\n                destination=\"26.8,80.9\",\n                o_lat=28.6,\n                o_lon=77.2,\n                d_lat=26.8,\n                d_lon=80.9,\n                router_name=\"north\",\n                router_host=\"http://router:5000\",\n                selected_origin_snap_m=10.0,\n                selected_destination_snap_m=15.0,\n                used_relaxed_coverage=False,\n                used_global_fallback=False,\n            )\n        self.assertEqual(result[\"provider\"], \"osrm\")\n        self.assertEqual(result[\"router_name\"], \"north\")\n        self.assertGreater(result[\"distance_km\"], 0)\n        self.assertGreater(result[\"duration_min\"], 0)\n        self.assertEqual(result[\"steps\"], [\"Head north\"])\n\n    def test_run_osrm_route_outside_coverage(self):\n        payload = {\n            \"routes\": [\n                {\n                    \"distance\": 1200.0,\n                    \"duration\": 300.0,\n                    \"legs\": [{\"steps\": []}],\n                    \"geometry\": {\"coordinates\": [[77.2, 28.6], [80.9, 26.8]]},\n                }\n            ],\n            \"waypoints\": [{\"distance\": 25000.0}, {\"distance\": 30000.0}],\n        }\n        with patch(\"backend.routers.route.httpx.Client\", return_value=_DummyClient(response=_DummyResponse(payload=payload))):\n            result = route_module.run_osrm_route(\n                origin=\"28.6,77.2\",\n                destination=\"26.8,80.9\",\n                o_lat=28.6,\n                o_lon=77.2,\n                d_lat=26.8,\n                d_lon=80.9,\n                router_name=\"north\",\n                router_host=\"http://router:5000\",\n                selected_origin_snap_m=10.0,\n                selected_destination_snap_m=15.0,\n                used_relaxed_coverage=False,\n                used_global_fallback=False,\n            )\n        self.assertEqual(result[\"code\"], \"outside_osrm_coverage\")\n        self.assertEqual(result[\"router_name\"], \"north\")\n\n    def test_run_osrm_route_no_routes(self):\n        payload = {\"routes\": []}\n        with patch(\"backend.routers.route.httpx.Client\", return_value=_DummyClient(response=_DummyResponse(payload=payload))):\n            result = route_module.run_osrm_route(\n                origin=\"28.6,77.2\",\n                destination=\"26.8,80.9\",\n                o_lat=28.6,\n                o_lon=77.2,\n                d_lat=26.8,\n                d_lon=80.9,\n                router_name=\"north\",\n                router_host=\"http://router:5000\",\n                selected_origin_snap_m=10.0,\n                selected_destination_snap_m=15.0,\n                used_relaxed_coverage=False,\n                used_global_fallback=False,\n            )\n        self.assertEqual(result[\"error\"], \"No route found\")\n\n    def test_run_osrm_route_invalid_metrics(self):\n        payload = {\n            \"routes\": [\n                {\n                    \"distance\": 0.0,\n                    \"duration\": 300.0,\n                    \"legs\": [{\"steps\": []}],\n                    \"geometry\": {\"coordinates\": [[77.2, 28.6], [80.9, 26.8]]},\n                }\n            ],\n            \"waypoints\": [{\"distance\": 12.0}, {\"distance\": 16.0}],\n        }\n        with patch(\"backend.routers.route.httpx.Client\", return_value=_DummyClient(response=_DummyResponse(payload=payload))):\n            result = route_module.run_osrm_route(\n                origin=\"28.6,77.2\",\n                destination=\"26.8,80.9\",\n                o_lat=28.6,\n                o_lon=77.2,\n                d_lat=26.8,\n                d_lon=80.9,\n                router_name=\"north\",\n                router_host=\"http://router:5000\",\n                selected_origin_snap_m=10.0,\n                selected_destination_snap_m=15.0,\n                used_relaxed_coverage=False,\n                used_global_fallback=False,\n            )\n        self.assertEqual(result[\"code\"], \"invalid_osrm_route_metrics\")\n\n    def test_run_osrm_route_handles_request_error(self):\n        err = httpx.RequestError(\"boom\", request=httpx.Request(\"GET\", \"http://router:5000\"))\n        with patch(\"backend.routers.route.httpx.Client\", return_value=_DummyClient(error=err)):\n            result = route_module.run_osrm_route(\n                origin=\"28.6,77.2\",\n                destination=\"26.8,80.9\",\n                o_lat=28.6,\n                o_lon=77.2,\n                d_lat=26.8,\n                d_lon=80.9,\n                router_name=\"north\",\n                router_host=\"http://router:5000\",\n                selected_origin_snap_m=10.0,\n                selected_destination_snap_m=15.0,\n                used_relaxed_coverage=False,\n                used_global_fallback=False,\n            )\n        self.assertIn(\"OSRM unavailable\", result[\"error\"])\n\n    def test_run_osrm_route_handles_http_error(self):\n        with patch(\n            \"backend.routers.route.httpx.Client\",\n            return_value=_DummyClient(response=_DummyResponse(status_code=503)),\n        ):\n            result = route_module.run_osrm_route(\n                origin=\"28.6,77.2\",\n                destination=\"26.8,80.9\",\n                o_lat=28.6,\n                o_lon=77.2,\n                d_lat=26.8,\n                d_lon=80.9,\n                router_name=\"north\",\n                router_host=\"http://router:5000\",\n                selected_origin_snap_m=10.0,\n                selected_destination_snap_m=15.0,\n                used_relaxed_coverage=False,\n                used_global_fallback=False,\n            )\n        self.assertEqual(result[\"error\"], \"OSRM error (503).\")\n\n\nif __name__ == \"__main__\":\n    unittest.main()\n",
      "data": "import unittest\nfrom unittest.mock import patch\n\nimport httpx\ntry:\n    from fastapi import HTTPException\nexcept ModuleNotFoundError:  # pragma: no cover\n    HTTPException = None\n\ntry:\n    from backend.routers import route as route_module\nexcept ModuleNotFoundError as e:  # pragma: no cover\n    if getattr(e, \"name\", \"\") == \"fastapi\":\n        route_module = None\n    else:\n        raise\n\n\nclass _DummyResponse:\n    def __init__(self, status_code=200, payload=None):\n        self.status_code = status_code\n        self._payload = payload or {}\n        self.request = httpx.Request(\"GET\", \"http://example.test\")\n\n    def raise_for_status(self):\n        if self.status_code >= 400:\n            raise httpx.HTTPStatusError(\n                \"error\",\n                request=self.request,\n                response=httpx.Response(self.status_code, request=self.request),\n            )\n\n    def json(self):\n        return self._payload\n\n\nclass _DummyClient:\n    def __init__(self, response=None, error=None):\n        self._response = response or _DummyResponse()\n        self._error = error\n\n    def __enter__(self):\n        return self\n\n    def __exit__(self, exc_type, exc, tb):\n        return False\n\n    def get(self, *_args, **_kwargs):\n        if self._error:\n
- Generated diff:
- None
- Raw model responses:
- {"tool":"read_map_platform_file","args":{"path":"tests/test_route_contracts.py"},"reason":"Custom router and route QA are the best bounded next focus for the routing agent. Reading the full route contract test file is the fastest way to identify a small, commit-worthy reliability gap before proposing or editing any router/backend code."}
---
{"tool":"read_map_platform_file","args":{"path":"backend/routers/route.py"},"reason":"Inspect the router implementation corresponding to the route contract tests to identify the smallest bounded reliability fix or missing edge-case test to complete this development run."}
{"tool":"read_map_platform_file","args":{"path":"backend/routers/route.py"},"reason":"Inspect the router implementation corresponding to the route contract tests to identify the smallest bounded reliability fix or missing edge-case test to complete this development run."}
---
{"tool":"run_map_platform_verify","args":{"timeout_ms":120000},"reason":"Validate the current route contracts against the existing implementation before making any scoped change; this is the smallest next action to confirm whether there is an actionable failing gap to fix."}
{"tool":"run_map_platform_verify","args":{"timeout_ms":120000},"reason":"Validate the current route contracts against the existing implementation before making any scoped change; this is the smallest next action to confirm whether there is an actionable failing gap to fix."}
---
{"tool":"search_map_platform","args":{"query":"skipIf(HTTPException is None or route_module is None", "limit": 20},"reason":"Verification skipped all route contract tests due to missing FastAPI in this environment. Search for similar skip patterns or import-fallback tests to identify a small, commit-worthy improvement that can make router contract coverage runnable here."}
{"tool":"search_map_platform","args":{"query":"skipIf(HTTPException is None or route_module is None","limit":20},"reason":"Verification skipped all route contract tests due to missing FastAPI in this environment. Search for similar skip patterns or import-fallback tests to identify a small, commit-worthy improvement that can make router contract coverage runnable here."}
---
{"tool":"finish","args":{"result":"blocked","notes":"Current bounded next task appears to be making route contract coverage runnable without FastAPI installed, but that would require editing tests and likely adding local stubs or restructuring imports across backend/routers/route.py and tests/test_route_contracts.py. With no prior approved proposal/change request in this continued run and no explicit user objective beyond continuing, I’m stopping rather than initiating a broader multi-file rewrite. Existing verification currently passes only via skipped tests."}}
{"tool":"finish","args":{"result":"blocked","notes":"Current bounded next task appears to be making route contract coverage runnable without FastAPI installed, but that would require editing tests and likely adding local stubs or restructuring imports across backend/routers/route.py and tests/test_route_contracts.py. With no prior approved proposal/change request in this continued run and no explicit user objective beyond continuing, I’m stopping rather than initiating a broader multi-file rewrite. Existing verification currently passes only via skipped tests."}}
- $ git worktree add --detach /tmp/map-platform-agent-hjxh_99a/worktree HEAD
cwd: /Users/abhisheksrivastava/map_platform
exit: 0
stdout:
HEAD is now at cce6af8 agent-run: map-platform host iteration
stderr:
Preparing worktree (detached HEAD cce6af8)
- $ git worktree remove --force /tmp/map-platform-agent-hjxh_99a/worktree
cwd: /Users/abhisheksrivastava/map_platform
exit: 0
- $ git worktree prune
cwd: /Users/abhisheksrivastava/map_platform
exit: 0
- No patch generated.
- No finalization attempted because no verified patch was applied.
- Canonical repo was not modified until generated diff passed validation and verification.

## Git Finalization

- No finalization attempted because no verified patch was applied.
- Canonical repo was not modified until generated diff passed validation and verification.

## Blockers

- No repository files changed.
